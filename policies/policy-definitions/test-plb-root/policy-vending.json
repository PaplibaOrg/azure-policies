{
  "displayName": "Enforce Subscription Name Format",
  "policyType": "Custom",
  "mode": "All",
  "description": "Ensures all subscriptions follow the naming convention: <companyPrefix>-<application-name>-dev/test/prod-<sequence> where companyPrefix is a parameter and sequence is exactly 3 digits",
  "metadata": {
    "category": "Naming",
    "version": "1.0.0"
  },
  "parameters": {
    "companyPrefix": {
      "type": "String",
      "metadata": {
        "displayName": "Company Prefix",
        "description": "The company prefix for subscription names. This parameter is required and must be provided when assigning the policy."
      }
    }
  },
  "policyRule": {
    "if": {
      "allOf": [
        {
          "field": "type",
          "equals": "Microsoft.Resources/subscriptions"
        },
        {
          "field": "name",
          "not": {
            "match": "[concat('^', parameters('companyPrefix'), '-[a-zA-Z0-9_-]+-(dev|test|prod)-[0-9]{3}$')]"
          }
        }
      ]
    },
    "then": {
      "effect": "deny"
    }
  }
}

